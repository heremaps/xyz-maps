{
    "layers": [{
        "id": "spaceLayer",
        "provider": {
            "id": "spaceProvider",
            "type": "SpaceProvider",
            "name": "my SpaceProvider",
            "level": 14,
            "space": "${SPACEID}",
            "url": "${URL}",
            "credentials": "${CREDENTIALS}"
        },
        "min": 14,
        "max": 20
    }]
}